#include <stdio.h>

int main (){
	unsigned long int f, s, denominador; 
	int i, n;
	denominador = 1;
	for (i=1; i<=50; i++){
		f = 1;
		for (n=i; n>0; n--)
			f *= n;
		denominador

		s = 
		if (i % 2 == 0)
			s = s + (f)/i;
	printf("%l\n",f);
	}
	return 0;
	
}
